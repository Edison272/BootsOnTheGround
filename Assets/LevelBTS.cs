using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class LevelBTS : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        Debug.Log("Parallel Scene Running!");
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
